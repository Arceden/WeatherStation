package data;

public class DataFrame {

    /*
     * Not sure about using this method of datasharing. We might convert the whole dataset to a bytearray before sending
     * or we will send the whole StorageRecord to the Storage server.
     */
    public static byte[] toDataFrame(StorageRecord storageRecord){

        byte[] dataframe = new byte[33];

        return new byte[1];

    }

}
